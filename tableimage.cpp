#include "tableimage.hpp"


TableImage::TableImage()
{
    // QWidget* w = new QWidget();
    // QLabel* _l = new QLabel();
    // _l->setPixmap(QPixmap(data[i-1]));
    // QVBoxLayout* lay = new QVBoxLayout;
    // lay->addWidget(_l);
    // w->setLayout(lay);
    // t->setCellWidget(t->last_row, i-1, w);
}
